#réglage serveur
  serveur :
    port: 10000

#nom appli
  spring :
    application :
      name : zezuperbank
      #accés BD
      datasource :
        url: http://localhost/phpmyadmin/db_structure.php?server=2&db=zezuperbank:3308
        username: root
        password:
      jpa:
        database-platform: org.hibernate.dialect.MySQL5Dialect
        hibernate:
          #ne pas laisser en prod#
          ddl-auto: create-drop

